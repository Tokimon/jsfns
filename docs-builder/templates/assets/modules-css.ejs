<style>
  table {
    table-layout: auto;
    border-collapse: collapse;
  }

  table th,
  table td {
    text-align: left;
    padding: 5px 10px;
    border: 0 solid #444;
    border-width: 0 0 1px;

    & + & {
      border-left-width: 1px;
    }
  }

  table tbody tr:last-child th,
  table tbody tr:last-child td {
    border-bottom: 0;
  }

  details[open] summary {
    margin-bottom: 5px;
  }

  details pre code.hljs {
    background-color: #1c2431;
    box-shadow: inset #141a24 0 0 5px;
  }

  summary {
    font-size: 12px;
    cursor: pointer;
    color: gray;
    transition: color 0.2s;
  }

  summary:hover,
  summary:focus {
    color: #cc7000;
    outline: none;
  }

  pre code.hljs {
    padding: 15px;
  }

  code:not(.hljs) {
    color: hotpink;
  }

  .page {
    --header-hight: 50px;

    grid-template-areas:
      'header header'
      'nav content';
    grid-template-columns: 300px 1fr;
  }

  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 99;
    background-color: #212d36;
    color: white;
    padding: 0 10px;
    grid-area: header;
    height: var(--header-hight);
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .page-title {
    margin: 0;
    font-size: 18px;
    flex: 1 1 100%;
    display: flex;
    gap: 10px;
    align-items: center;
  }

  .page-title a {
    color: currentColor;
    text-decoration: none;
    padding: 5px 10px;
    border: 1px solid #444;
    transition: background-color 0.2s;
    border-radius: 2px;
  }

  .page-title a:hover,
  .page-title a:focus,
  .page-title a.selected {
    background: #58b29a;
  }

  .page-version {
    white-space: nowrap;
  }

  .modules {
    grid-area: content;
    padding: 0 15px var(--header-hight);
    overflow: hidden;
    position: relative;
    z-index: 9;
    container-type: inline-size;
  }

  .menu-trigger {
    width: 22px;
    height: 16px;
    border: 0;
    background: linear-gradient(white, white 25%, transparent 25%, transparent);
    background-repeat: repeat;
    background-size: 1px 7px;
    appearance: none;
    cursor: pointer;
  }

  .menu-trigger,
  .repository {
    flex: 0 0 auto;
  }

  .repository img {
    width: 20px;
    aspect-ratio: 1;
  }

  @media (min-width: 800px) {
    .page {
      display: grid;
    }

    .menu-trigger {
      display: none;
    }
  }

  .menu {
    grid-area: nav;
    position: fixed;
    z-index: 98;
    width: 300px;
    top: var(--header-hight);
    bottom: 0;
    left: 0;
    background: #192229;
    overflow-y: auto;
    overflow-x: hidden;
    translate: -100% 0;
    transition: translate 0.3s ease;
    font-size: 18px;
  }

  .menu.open {
    translate: 0;
  }

  .menu-list {
    margin: 0;
    padding: 0;
    list-style: none;
    padding: 10px 0;
    box-shadow: inset 0 0 0 transparent;
  }

  .menu-list-item a {
    color: #ccc;
    text-decoration: none;
    padding: 10px;
    display: block;
    transition: all 0.2s;
  }

  .menu-list-item.current a {
    background: rgba(122, 150, 174, 0.25);
    box-shadow: inset 5px 0 0 cornflowerblue;
    padding-left: 20px;
  }

  .menu-list-item a:hover,
  .menu-list-item a:focus {
    background: rgba(255, 255, 255, 0.15);
    outline: none;
  }

  @media (min-width: 800px) {
    .menu {
      translate: 0;
    }
  }

  .module {
    padding-top: var(--header-hight);
  }

  .module-content {
    background: #192229;
    border-radius: 5px;
    margin-top: 15px;
    transition: border-color 1s;
    border: 1px solid #444;
  }

  .module-content:hover {
    border-color: darkolivegreen;
    transition: border-color 0.2s;
  }

  .module:target .module-content,
  .module:target .module-content:hover {
    border-color: cornflowerblue;
  }

  .module-content h1 {
    margin: 0;
    padding: 0 15px;
    line-height: 50px;
    font-size: 18px;
    font-weight: 400;
    border-top-left-radius: inherit;
    border-top-right-radius: inherit;
  }

  .module-content h1 a {
    color: currentColor;
    text-decoration: none;
  }

  .module-content h1 a:hover b,
  .module-content h1 a:focus b {
    color: currentColor;
  }

  .module-content h1 b {
    color: gray;
    transition: color 0.2s;
  }

  .signatures {
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  @container (min-width: 600px) {
    .module-content h1 {
      font-size: 25px;
    }
  }

  .signature-content {
    display: flex;
    flex-direction: column;
    gap: 15px;
    padding: 15px;
  }

  .signature-definition {
    font-size: 14px;
    border: 0 solid #444;
    border-width: 1px 0;
  }

  .signature-description {
    font-size: 18px;
    color: #aaa;
    display: flex;
    flex-direction: column;
    gap: 10px;
  }

  .signature-remark {
    padding: 10px;
    border: currentColor 1px solid;
    color: darkorange;
    border-radius: 4px;
  }

  @container (min-width: 750px) {
    .signature-definition {
      font-size: 20px;
    }
  }

  .tooltip {
    display: none;
    border-radius: 5px;
    box-shadow: #1a1f23 0 4px 8px -2px;
    overflow: hidden;
    border: 1px solid #333;
    z-index: -1;
    position: absolute;
  }

  .tooltip code.hljs {
    background-color: #292d25;
  }

  .tooltip.show {
    display: block;
  }
</style>
