---
"@jsfns/web": major
---

- **BREAKING:** The general-purpose types `NotFirst`, `Maybe`, and `Dictionary` have moved to `@jsfns/core`. Import them from `@jsfns/core/types.js` instead of `@jsfns/web/types.js`. Web-specific types (`GeneralWindow`, `EventName`, `ActualEvent`, `EventHandler`, `Size`) remain in `@jsfns/web/types.js`.
