---
"@jsfns/core": minor
---

- Added `MicroEvents`, a simple, environment-agnostic typed event bus (`subscribe`, `unsubscribe`, `unsubscribeAll`, `trigger`), with `once` and `when` subscription options
